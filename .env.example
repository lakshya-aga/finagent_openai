# ── LLM Providers ──────────────────────────────────────────────────────
# Configure at least one provider that matches your FINAGENT_DEFAULT_MODEL
# / role overrides. Supported provider prefixes: openai, anthropic, google,
# gemini, ollama.
OPENAI_API_KEY=sk-your-key-here
# ANTHROPIC_API_KEY=sk-ant-your-key
# GOOGLE_API_KEY=your-google-key
# OLLAMA_BASE_URL=http://localhost:11434

# Unified model overrides (optional — defaults are set in finagent/llm.py).
# Use provider:model specs. Plain OPENAI_MODEL remains supported for legacy
# OpenAI-only deployments.
# FINAGENT_DEFAULT_MODEL=openai:gpt-5-mini
# INTENT_CLASSIFIER_MODEL=anthropic:claude-haiku-4-5
# CHAT_QUESTION_MODEL=ollama:qwen2.5:14b
# OPENAI_MODEL=gpt-5-mini

# Workflow runtime: langgraph routes provider-neutral branches and delegates
# legacy MCP tool loops until those tools are fully LangChain-native.
# FINAGENT_WORKFLOW_RUNTIME=langgraph

# ── MCP Server URLs ────────────────────────────────────────────────────
# Standalone dev usually runs data-mcp on 8001 to avoid colliding with
# finagent's default port 8000. Synapse's docker-compose overrides these
# with in-network service URLs.
FRUIT_THROWER_URL=http://localhost:8090/mcp/
DATA_MCP_URL=http://localhost:8001/sse

# ── Knowledge Store / File Search ───────────────────────────────────────
# openai = OpenAI Files + Vector Stores, local = persist PDFs locally without
# hosted semantic file-search tools.
# KNOWLEDGE_STORE_BACKEND=openai
# OPENAI_VECTOR_STORE_ID=vs_your_store_id
# KNOWLEDGE_STORE_PATH=outputs/knowledge

# ── Rate Limiting ──────────────────────────────────────────────────────
# Max requests per minute per client on expensive endpoints.
# Set to 0 to disable rate limiting.
RATE_LIMIT_RPM=10

# ── Session Persistence ────────────────────────────────────────────────
# SQLite DB path for session storage. Default: outputs/sessions.db
# SESSION_DB_PATH=outputs/sessions.db
# Days before stale sessions are auto-cleaned. Default: 7
# SESSION_TTL_DAYS=7

# ── Experiment Store ───────────────────────────────────────────────────
# Path to the SQLite experiments database.
# FINAGENT_EXPERIMENT_DB=outputs/experiments.db

# ── Debate Configuration ──────────────────────────────────────────────
# Set to '1' to use the legacy bull/bear/moderator debate flow.
# DEBATE_LEGACY=0

# ── Trading Panel (LangGraph multi-agent debate) ───────────────────────
# Default LLM for panel agents. Supports openai, anthropic, ollama, google.
# PANEL_DEFAULT_MODEL=ollama:qwen3:4b

# ── Phoenix Tracing (LLM observability) ────────────────────────────────
# Set to enable OpenTelemetry-based tracing. No-op if unset.
# PHOENIX_COLLECTOR_ENDPOINT=http://phoenix:4317
# PHOENIX_PROJECT_NAME=synapse

# ── Lineage ─────────────────────────────────────────────────────────────
# Timeout for runtime lineage extraction (seconds). Default: 180
# FINAGENT_LINEAGE_TIMEOUT=180
